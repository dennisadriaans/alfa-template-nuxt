export default defineAppConfig({
  ui: {
    button: {
      slots: {
        base: ["cursor-pointer"],
      },
    },
    select: {
      slots: {
        base: ["cursor-pointer"],
      },
    },
    colors: {
      primary: "green",
      neutral: "neutral",
    },
  },
});
